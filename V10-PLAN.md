# V10 计划：词库大扩张 + 分词书系统

> 状态：**方案设计（未实施）**。目标版本 v10，在 v9 上线之后再动工，作为「第十个版本的大词库扩张」。
> 用户需求原文：更新一次之后，在第十个版本做大的词库扩张，可以分词书；用户可自由选择词书进行练习和背诵；词书内容在网站导航中随时可查、没有重复。

## 1. 目标
1. 词库大扩张（从 2274 词扩到 3000+，分多本词书）
2. 用户可自由选择词书进行练习和背诵
3. 词书内容在首页 / 导航「随时可查，没有重复」
4. 不破坏现有 2274 词的统计 / 生词本 / 云同步数据（向后兼容）

## 2. 数据层方案

### 2.1 词条新增 `book` 字段
```js
{t:'core', w:'abandon', s:['give up','quit'], c:'放弃', e:'...', k:'give up', p:'...', pos:'v.', d:'...', z:'l', book:'core-300'}
```
- `book`：词书 id，如 `core-300` / `listening-500` / `writing-400` / `academic-500` / `travel-200` …
- 老词默认 `book:'default'`（或按现有主题自动归组），旧数据照常工作
- 一本词书可跨主题、跨专区；**一个词只属于一本词书**（不重复）

### 2.2 词书定义（新文件 `work/books.js`）
```js
const BOOKS = [
  {id:'core-300',    name:'高频核心 300',  desc:'雅思高频通用词', icon:'⭐', zone:'both', words:300},
  {id:'listening-500',name:'听力真题 500', desc:'听力高频同义替换', icon:'🎧', zone:'l', words:500},
  {id:'writing-400', name:'写作替换 400',  desc:'书面语升级替换', icon:'✍️', zone:'w', words:400},
  {id:'academic-500',name:'学术阅读 500',  desc:'学术场景词汇',   icon:'📚', zone:'both', words:500},
  {id:'travel-200',  name:'旅行场景 200',  desc:'旅行出行高频',   icon:'✈️', zone:'both', words:200},
  // …按词书来源持续扩充
];
```
- `zone` 控制该书默认落在听力 / 书写 / 通用专区

## 3. UI 方案

### 3.1 词书选择入口（三处）
- 首页：词书卡区升级为词书列表，点卡片进入词书详情
- 词库页：新增「词书」筛选（与专区 × 主题筛选并列，三选一联动）
- 闪卡 / 练习：选择模式前先选词书（记住上次选择）

### 3.2 词书详情页（站内视图，不跳新页面）
- 封面卡：图标 / 名称 / 词数 / 描述 / 进度（已背 x / y）
- 「开始练习」「开始背诵」按钮
- 该书词条列表（可搜索，随时可查）
- 顶部保留全局导航，可随时切回其他词书或词库

### 3.3 不重复保证
- 词条唯一归属一本词书；词库页「全部」视图显示所有词，具体词书视图只显示该书
- 首页 / 导航与练习共用同一 `WORDS` 数据源，不会出现两份数据
- 构建期校验：词无重复、`book` 都存在、每本书词数与定义一致

## 4. 练习 / 背诵接入
- 闪卡：选词书后只洗该书词汇（翻面 / 发音 / 详情不变）
- 听写 / 看词选义 / 听音选义 / 选择题 / 配对题：范围改为所选词书
- 背诵模式（新增）：选词书 → 顺序 / 乱序 → 逐词背诵 → 标记「已会 / 再背」→ 进度存入 `localStorage['ielts-syn-trainer-v1']`（扩展字段，向后兼容）
- 统计页：新增「词书维度」统计（每本进度 / 掌握率 / 生词数）

## 5. 实施步骤
1. 规划词书清单与词数（每本书的来源：剑桥真题词表 / 雅思核心词汇表 / 场景词表）
2. 新建 `work/books.js` + 批量补词（先按词书分批写词条文件，如 `work/v10-book-*.js`）
3. 词条加 `book` 字段；老词归 `default`
4. `work/build.js` 接入 books.js，构建时校验（无重复 / book 合法 / 词数正确）
5. UI：词书选择器（首页 / 词库 / 闪卡 / 练习）
6. 新增词书详情视图 + 背诵模式
7. 统计页词书维度
8. 截图验证 + `work/e2e-v7.ps1` 回归
9. 用户确认后构建、提交、`node work/push-gh.js` 上线

## 6. 需要用户提供 / 申请的东西
- 词书内容来源：**不需要申请**，用公开雅思词表整理即可；如果你手上有特定词书（如某机构的词汇书 PDF/表格），发我文件更好
- 网站本身是纯静态站，免费托管，**无额外申请**；分词书不需要服务器
- 如果以后想接「在线词典发音 / AI 释义」，才需要申请第三方 API key（可暂不接，现有发音为浏览器 TTS）

## 7. 风险与对策
- 词量变大后单文件变大：构建时字段压缩，只保留必要字段；若超过可接受范围再考虑按词书懒加载（备选方案，先不拆）
- 旧数据兼容：`book` 缺省为 `default`，老词库 / 老用户进度不受影响
- 云同步：localStorage 扩展字段向后兼容，云端 `user_data` 表结构不变
- 统计口径变化：统计页新增维度，旧维度（总量 / 专区）保持不变

## 8. 里程碑
- M1：词书框架（books.js + book 字段 + 构建校验）→ 先把现有 2274 词按主题归入 4~5 本初始词书
- M2：UI 词书选择 + 词书详情（首页 / 词库 / 闪卡 / 练习全接入）
- M3：背诵模式 + 词书维度统计
- M4：批量补词到 3000+，校验 + 回归 + 上线