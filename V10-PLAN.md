# V10 计划：词书系统 + 词库大扩张（合并版 · 可执行实施计划）

> 状态：**功能与词书全部完成，已推送上线**（2026-08-08）。M1~M4 已推送上线；M5 词条全部接入并推送上线（listening 821 / jianqiao 450 / zhenjing 938 / awl 570 / band9 102 / oxford 1000），总词数 5622；实施记录：本地 git + 各批次 validate 脚本与 e2e。

## 0. 目标
1. 词库从 2274 扩到约 4500–5500 词，分 7 本词书
2. 用户可自由选择词书进行练习和背诵，词书内容在导航中随时可查、**无重复**
3. 伪页面跳转（哈希路由 + 转场动画）：URL 变化、支持前进/后退/分享链接，保持单文件架构
4. 新增背诵模式；统计/详情/设置全面感知词书维度
5. 全部来源为 MIT/公开许可，**不整抄商业书内容**

## 1. 词书清单（7 本）
| id | 名称 | 数据来源 | 许可 | 目标词数 | 净增估算 |
|---|---|---|---|---|---|
| `default` | 内置词库 | 现有 2274 词 | 自有 | 2274 | 0 |
| `jianqiao` | 真题高频 | chunsi-w/ielts-vocab-cloudflare | MIT | 500–800 | 400–800 |
| `listening` | 听力拼写 | yuqingsong71/IELTS-listening-vocabulary（剑桥 4-20 听力填空答案词） | 公开 | 600–1000 | ~600 |
| `zhenjing` | 雅思真经 | hefengxian/ielts-vocabulary | MIT | ~1674 | 800–1200 |
| `awl` | 学术词汇 AWL | Coxhead，惠灵顿维多利亚大学 | 开放教育免费使用 | 570 | 400–500 |
| `band9` | Band 9 高分表达 | learning-zone/ielts-materials | MIT | 200–300 | 200–300 |
| `oxford` | 牛津基础词 | OUP 牛津 3000 | 需署名 + 非商用 | ~3000 | 500–1000 |

- 净增合计目标：**2000–3000**（总量约 4500–5500）；牛津 3000 与现有基础词重叠较多，净增以去重后为准
- `oxford` 正式接入；与现有词重叠多属正常（用户可选学基础词或跳过），设置页来源区必须附 OUP 署名 + 非商用声明

## 2. 数据来源与许可（合规红线）
- 仅使用上表列出的 MIT / 公开许可来源；从 GitHub 拉取原始数据时记录 commit/版本
- **禁止整抄**：新东方红宝书、王陆《雅思王听力》、刘洪波《阅读 538》等商业出版物转录来源
- 设置页新增「词书来源」区：列出每本书来源 + 许可声明；`oxford` 必须附 OUP 署名与「非商用」声明

## 3. 数据模型
### 3.1 词条新增 `b`（词书）字段，**数组、一词多书**
```js
{t:'env', w:'biodiversity', s:['species diversity','ecosystem variety'], c:'生物多样性', e:'The region is famous for its biodiversity.', k:'biodiversity', p:'...', pos:'n.', d:'...', z:'l', b:['zhenjing','jianqiao','awl']}
```
- 一个词可属于多本书 → 词库中**只有一条数据，无重复**；筛选某本书 = 范围过滤，不是复制
- 老词默认 `b:['default']`（后续可按主题/专区细分归组）
- 每本书可跨主题、跨专区

### 3.2 词书定义（新文件 `work/books.js`）
```js
const BOOKS = [
  {id:'default',  name:'内置词库',   desc:'网站原有全部词', icon:'📖', zone:'both', source:'自有', license:'自有', words:2274},
  {id:'jianqiao', name:'真题高频',   desc:'剑桥 4-19 真题高频词', icon:'📚', zone:'both', source:'chunsi-w/ielts-vocab-cloudflare', license:'MIT', words:500},
  // ...
];
module.exports = { BOOKS };
```
- `zone` 默认专区；`source/license` 供设置页来源区展示
- 构建期校验：词条 `b` 内 id 均存在于 BOOKS；`default` 至少覆盖现有 2274 词；每本书词数与定义一致（不一致仅警告，因一词多书总和 > 词数）

## 4. 站点功能
1. **书架页（新 tab「📚 词书」）**：BOOKS 卡片列表（名称/来源/词数/你的进度/许可），点击进入词书详情页
2. **词书详情页**：封面信息 + 该书词列表（可搜索）+「开始背诵」「开始练习」按钮
3. **词库页**：新增第三个筛选「词书」（与专区/主题并列）
4. **闪卡 / 练习**：设置里新增词书选择，作用域锁定所选词书；默认「全部词库」
5. **统计页**：新增词书维度（每本进度/掌握率/生词数）
6. **详情弹窗**：显示词书标签（可点击 → 词库页按该书筛选）
7. **设置页**：新增「词书来源」说明区（各来源 + 许可声明；`oxford` 必须附 OUP 署名与「非商用」声明）

## 5. 背诵模式（新增）
- 选词书 → 顺序/乱序 → 逐词展示（词 + 释义 + 例句）→ 认识 / 不认识
- 不认识的词进入**本轮重复队列**，背完一轮统计正确率，可再背错词
- 进度存入 `localStorage['ielts-syn-trainer-v1']` 扩展字段（向后兼容，云同步结构不变）

## 6. 伪页面跳转（哈希路由 + 转场动画）
- URL 形如 `#/learn`、`#/flash`、`#/book/jianqiao`、`#/stats`、`#/settings`
- tab 点击改为写 hash；`hashchange` 监听切换 view；沿用现有 `.view.active` 的 `viewIn` 转场
- 支持浏览器前进/后退、收藏/分享词书页链接
- GitHub Pages 零配置；保持单文件架构，进度/生词本/登录状态不掉线

## 7. 分批实施路线（里程碑）
- **M1 数据层**：新建 `work/books.js`；词条加 `b` 字段（老词归 `default`）；build.js 接入并校验；不新增任何词，先构建通过
- **M2 词书 UI**：书架页 + 词书详情页 + 词库第三筛选 + 闪卡/练习词书选择 + 统计词书维度 + 详情弹窗书标签 + 设置来源区（先用现有词跑通）
- **M3 背诵模式**：选书 → 顺序/乱序 → 认识/不认识 → 重复队列 → 正确率
- **M4 哈希路由**：伪页面跳转 + 转场 + 前进/后退
- **M5+ 词条接入（每批 400–500，顺序如下）**
  1. `listening` 听力拼写（~600 净增）
  2. `jianqiao` 真题高频首批（~400）
  3. `zhenjing` 真经（~800–1200，可分 2–3 批）
  4. `awl`（~400–500）
  5. `band9`（~200–300）
  6. `oxford` 牛津基础（去重后净增 500–1000，可分 2 批）
- **M6 收尾**：全量校验 + `e2e-v7` 回归 + 新增词书筛选 e2e + 截图 → 用户确认后推送

## 8. 词条生产与校验流程（每批必做）
1. 从来源仓库拉取原始数据，解析为词条草案（`w/c/e/d/p/pos/t/z` 尽量直接用来源字段，缺失的补全）
2. AI 起草同义词组 `s` 与例句关键词 `k`；**`k` 必须逐字出现在 `e` 中**
3. `t` 只用 10 个标准主题 id（core/edu/work/travel/health/env/living/academic/shopping/feelings），`z` 按专区
4. 新增批次词条写入独立文件（如 `work/v9-words.js` 之后的 `work/v10-book-*.js`），接入 build.js 前先跑校验脚本（对照 `work/.existing-words.txt` 与已接入批次去重）
5. `node work/build.js` → `Copy-Item outputs\index.html index.html -Force`
6. `work/e2e-v7.ps1` 回归 + 新增词书筛选断言 + 截图
7. 汇报本批词数变化，**推送前经用户确认**；确认后 `node work/push-gh.js`

## 9. 测试与上线
- 主回归：`work/e2e-v7.ps1`（14 项）；新增：词书筛选、书架导航、背诵模式、哈希路由
- 截图：书架页 / 词书详情 / 词库筛选 / 背诵 / 深色模式
- 上线：本地 commit → `node work/push-gh.js` → Pages Actions 自动部署（约 1–2 分钟）→ 线上带 `?_t=<时间戳>` 验证
- 最终推送时把更新后的 `V10-PLAN.md` 一并加入推送清单（作为实施记录）

## 10. 风险与对策
- 单文件从 ~620KB 涨到 ~1.2MB：仍可接受；构建压缩字段，虚拟滚动/分页可选
- 词条质量（`s/k` 是人工校验密集活）：分批 400–500、逐批校验，宁稳勿快
- 一词多书导致「每本书词数 = 定义数」不精确：校验改警告，以实际去重词数为准
- 牛津许可（必接）：设置页「词书来源」区必须附 OUP 署名 + 非商用声明；仅做基础词书，不用于任何商业用途
- 哈希路由与现有状态：进度/生词本在 localStorage，切换不掉线，云同步结构不变

## 11. 禁止事项
- 一次新增词条不得超过 500 条
- 不使用商业书转录来源
- 不改变 `push-gh.js` 机制与 GitHub Actions 部署 workflow
- 不删除 v8.0 原版备份（`backups/v8.0-original-20260807/`）
- 推送任何内容前必须先经用户确认